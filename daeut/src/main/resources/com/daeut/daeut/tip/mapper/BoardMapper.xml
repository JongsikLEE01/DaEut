<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.daeut.daeut.tip.mapper.BoardMapper">

    <select id="list" resultType="com.daeut.daeut.tip.dto.Board">
        SELECT 
            board_no AS boardNo,
            board_title AS boardTitle,
            board_content AS boardContent,
            board_reg_date AS boardRegDate,
            board_upd_date AS boardUpdDate,
            board_views AS boardViews,
            board_like AS boardLike,
            user_no AS userNo
        FROM 
            board
    </select>

    <select id="select" resultType="com.daeut.daeut.tip.dto.Board">
        SELECT 
            board_no AS boardNo,
            board_title AS boardTitle,
            board_content AS boardContent,
            board_reg_date AS boardRegDate,
            board_upd_date AS boardUpdDate,
            board_views AS boardViews,
            board_like AS boardLike,
            user_no AS userNo
        FROM 
            board
        WHERE board_no = #{boardNo}
    </select>

    <insert id="insert">
        INSERT INTO board (board_title, board_content, user_no)
        VALUES (#{boardTitle}, #{boardContent}, #{userNo})
    </insert>

    <update id="update">
        UPDATE board
           SET board_title = #{boardTitle}
              ,board_content = #{boardContent}
              ,board_upd_date = now()
        WHERE board_no = #{boardNo}
    </update>

    <delete id="delete">
        DELETE FROM board
        WHERE board_no = #{boardNo}
    </delete>

    <select id="maxPk" resultType="int">
        SELECT MAX(board_no)
        FROM board
    </select>

    <update id="view">
        UPDATE board
           SET board_views = board_views + 1
        WHERE board_no = #{boardNo}
    </update>
</mapper>
